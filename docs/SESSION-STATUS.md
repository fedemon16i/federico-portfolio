# Session status — 2026-08-08

## Applied (force CSS pass)

- EY: `--max` / `.page` forced **920**; hero no longer allowed 1100
- EY: `#win` transparent full stage; prototype children as **window**; **capbar** gradient-only (no gray slab)
- BC: `#demo` / `#process` forced into **920** column
- BC: phone sized with **fit script** (entire device visible, 9:19.5)

## Pending your QA

1. EY hero left/right edges align with PI  
2. EY measure beat: text → floating UI window → caption without gray bar  
3. BC demo same width as Problem  
4. BC phone fully visible, not clipped  

## Next phase

**Home Hero** (bio top, not sticky left) after Phase 0 passes QA.

## Docs not auto-synced to GitHub

Files live in this ZIP / artifacts until you `git add` + push.
