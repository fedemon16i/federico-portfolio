# Design tokens

Injected as `:root` variables in production HTML under the block `DESIGN SYSTEM TOKENS`.

Copy into the Design System page under **Tokens → Motion & Stage**.

See `ANIMATION-MOTOR-RULES.md` for usage rules and the bug log.
